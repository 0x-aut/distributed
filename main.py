# So this is where the routes will be? or we can use a route file to keep the routes there?

# First order of business will be to install the postgres package and ensure a connection can be made to the database
# Second order of business will be to build the workers to send things to the database in parallel (worker size will depend on CPU cores)
# Third order of business will be to build the workers to

def main():
  print("Hello from distributed!")
 